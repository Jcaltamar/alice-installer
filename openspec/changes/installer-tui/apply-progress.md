# Apply Progress: installer-tui

**Batch**: 1 (T-001..T-010)
**Mode**: Strict TDD
**Date**: 2026-04-18
**Status**: 10/84 tasks complete

---

## Completed Tasks

- [x] T-001 — Go module initialized at project root as `github.com/jcaltamar/alice-installer`, go 1.22.2
- [x] T-002 — Directory skeleton created with `.gitkeep` in all required dirs
- [x] T-003 — `tools.go` with `//go:build tools` pinning golangci-lint + goreleaser
- [x] T-004 — `Makefile` with all required targets
- [x] T-005 — `.golangci.yaml` conservative baseline (errcheck, govet, gofmt, staticcheck, unused, ineffassign, gosimple, revive)
- [x] T-006 — `internal/assets/assets.go` with go:embed + 8-test suite (unit) — all GREEN
- [x] T-007 — `internal/assets/compose_render_test.go` — 3 integration tests, confirmed RED on unrefactored files
- [x] T-008 — Refactored `internal/assets/docker-compose.yml` — parameterized images, removed runtime:nvidia, added redis, parameterized queue env
- [x] T-009 — Created `internal/assets/docker-compose.gpu.yml` GPU overlay
- [x] T-010 — Updated `internal/assets/.env.example` — blanked password, added image tag vars, REDIS_IMAGE, QUEUE_PORT

---

## Files Created / Modified

| File | Action | Description |
|------|--------|-------------|
| `go.mod` | Created | Module `github.com/jcaltamar/alice-installer`, go 1.22.2 |
| `go.sum` | Created | Auto-generated by `go get gopkg.in/yaml.v3` |
| `tools.go` | Created | Dev-dep pins under `//go:build tools` |
| `Makefile` | Created | All required targets |
| `.golangci.yaml` | Created | Conservative linter config |
| `internal/assets/assets.go` | Created | Embed directives for 4 assets |
| `internal/assets/assets_test.go` | Created | 8 unit tests |
| `internal/assets/compose_render_test.go` | Created | 3 compose render integration tests |
| `internal/assets/docker-compose.yml` | Created (copy+refactor) | Parameterized images, redis service, no GPU |
| `internal/assets/docker-compose.gpu.yml` | Created | NVIDIA GPU overlay |
| `internal/assets/.env.example` | Created (copy+update) | Blanked password, new image tag vars |
| `internal/assets/testdata/baseline_no_gpu.golden` | Created | Golden file for baseline compose render |
| `internal/assets/testdata/baseline_with_gpu.golden` | Created | Golden file for GPU overlay render |
| `openspec/changes/installer-tui/tasks.md` | Modified | Checked off T-001..T-010 |
| All `internal/*/gitkeep` dirs | Created | Scaffold directories |
| `.github/workflows/.gitkeep` | Created | CI directory placeholder |
| `testdata/.gitkeep` | Created | Golden files directory placeholder |

---

## TDD Cycle Evidence

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| T-006 | `internal/assets/assets_test.go` | Unit | N/A (new) | ✅ Written (compile error — no package) | ✅ 8/8 pass | ✅ 4 behaviors × 2 cases each | ➖ None needed |
| T-007 | `internal/assets/compose_render_test.go` | Integration | N/A (new) | ✅ Written (3 failures on unrefactored files) | ✅ 3/3 pass after T-008..T-010 | ✅ 3 cases (no-gpu, gpu, missing-password) | ➖ None needed |
| T-008..T-010 | (paired with T-007) | Integration | N/A (new files) | ➖ Paired with T-007 RED | ✅ T-007 passes | ➖ Paired with T-007 | ➖ None needed |

### Test Summary
- **Total tests written this batch**: 11 (8 unit + 3 integration)
- **Total tests passing**: 11
- **Layers used**: Unit (8), Integration (3)
- **Approval tests**: None — no refactoring of existing tested code
- **Pure functions created**: 2 (`withoutKey`, `composeConfigArgs`)

---

## Deviations from tasks.md

1. **T-001 module path**: tasks.md says `github.com/aliceguardian/installer` in `installer/` sub-dir. Orchestrator instructions + `module-identity` memory say `github.com/jcaltamar/alice-installer` at project root. **Used orchestrator value.**
2. **T-007 test location**: tasks.md says `testdata/compose_config_test.go`. Orchestrator batch instructions say `internal/assets/compose_render_test.go`. **Used orchestrator value.** Also, test file name differs (`compose_render_test.go` vs `compose_config_test.go`).
3. **tasks.md T-003/T-004/T-005 order vs batch instructions**: Batch instructions have Makefile as T-004, `.golangci.yaml` as T-005, `tools.go` as T-003 (different from tasks.md T-003=Makefile). Implemented per batch instruction ordering, checked off using original tasks.md numbering.
4. **`tools.go` `go mod tidy`**: tools.go imports `golangci-lint` and `goreleaser` packages. Running `go mod tidy` would download ~200MB of toolchain dependencies. Deferred to next batch when tools are actually needed; `tools.go` file exists with correct pattern.
5. **T-006 embedded GPU file**: tasks.md says GPU file is "empty stub for now". Batch instructions say to write the real GPU overlay in T-009 then update the embed. Created a minimal valid YAML stub for T-006 GREEN, then replaced with real content at T-009. Both asset tests remain GREEN throughout.

---

## Issues / Risks

- `tools.go` will cause `go mod tidy` to pull large toolchain dependencies. Consider adding `//go:build ignore` or a separate `_tools/go.mod`. Flag for batch 2.
- `go.sum` currently only has `gopkg.in/yaml.v3`. When bubbletea/lipgloss/teatest are added in a future batch, `go mod tidy` must be run.
- The `testdata/.gitkeep` in `internal/assets/testdata/` was removed when golden files were created — that's fine.

---

## Remaining Tasks

T-011..T-084 (74 tasks remaining). Next batch recommended: T-011..T-021 (Platform & Ports).
